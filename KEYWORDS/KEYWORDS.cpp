
//C++ KEYWORDS (C++20 Standard)

/*
alignas, alignof, and, and_eq, asm, auto, bitand, bitor, bool, break, case,
catch, char, char8_t, char16_t, char32_t, class, compl, concept, const, consteval,
constexpr, constinit, const_cast, continue, co_await, co_return, co_yield, decltype,
default, delete, do, double, dynamic_cast, else, enum, explicit, export, extern,
false, float, for, friend, goto, if, inline, int, long, mutable, namespace, new,
noexcept, not, not_eq, nullptr, operator, or, or_eq, private, protected, public,
register, reinterpret_cast, requires, return, short, signed, sizeof, static,
static_assert, static_cast, struct, switch, template, this, thread_local, throw,
true, try, typedef, typeid, typename, union, unsigned, using, virtual, void,
volatile, wchar_t, while, xor, xor_eq
*/

//Some are alternative tokens for symbols (like `and` = `&&`, `or` = `||`).

//COMMON C++ STANDARD LIBRARY FUNCTIONS & UTILITIES

// <iostream>, <iomanip>
/*
std::cin, std::cout, std::cerr, std::clog, std::endl,
std::fixed, std::setprecision(), std::setw()
*/

// <string>
/*
std::string, std::getline(), str.length(), str.substr(),
str.find(), str.replace(), str.append(), str.c_str()
*/

// <vector>
/*
std::vector, v.push_back(), v.pop_back(), v.size(),
v.begin(), v.end(), v.clear(), v.insert(), v.erase()
*/

// <algorithm>
/*
std::sort(), std::reverse(), std::count(), std::find(),
std::min(), std::max(), std::accumulate(), std::all_of(), std::any_of()
*/

// <cmath>
/*
std::pow(), std::sqrt(), std::ceil(), std::floor(), std::abs(),
std::sin(), std::cos(), std::tan(), std::log(), std::exp()
*/

// <utility>
/*
std::pair, std::make_pair(), std::swap(), std::move()
*/

// <map>, <set>, <unordered_map>, <unordered_set>
/*
std::map, std::set, std::unordered_map, std::unordered_set,
insert(), erase(), find(), count()
*/

// <queue>, <stack>, <deque>
/*
std::queue, std::stack, std::priority_queue, std::deque,
push(), pop(), front(), back(), top(), empty()
*/

// <functional>, <thread>, <mutex> (for advanced uses)

// NOTE:
// - C++ is backward-compatible with C, so most C functions (like printf, malloc) also work.
// - `std::` prefix means functions are from the C++ Standard Library (namespace std).
